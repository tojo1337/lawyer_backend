import { Router } from "express";

const route = Router();

route.get("/name-of-aourts", async (req, res)=>{
    // Add some code in here
});

route.get("/name-of-particulars", async (req, res)=>{
    // Add some code in here
});

route.get("/current-stages", async (req, res)=>{
    // Add soem cdoe in here
});

export { route };
